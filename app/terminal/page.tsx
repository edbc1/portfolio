import { Terminal } from "@/components/terminal/Terminal";

export const metadata = {
  title: "personal/os — terminal — ed bucaille",
  description: "Portfolio of Ed Bucaille — terminal edition.",
};

export default function TerminalPage() {
  return <Terminal />;
}
